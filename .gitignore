# Go binary (anchored: a bare `tq` would also ignore cmd/tq/)
/tq
dist/

# Task data of projects that use tq (never the tool's own repo)
# .tasks/ is meant to be committed in *your* project.

# JavaScript
node_modules/

# Environment
.env

# Coverage
coverage.html
coverage.out

# AI tools
.claude/
.cursor/

# macOS
.DS_Store

# Playwright MCP output
.playwright-mcp/
