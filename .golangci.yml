version: "2"

linters:
  settings:
    errcheck:
      # Writing to stdout/stderr is best-effort in a CLI: there is nowhere left
      # to report the failure to.
      exclude-functions:
        - fmt.Fprint
        - fmt.Fprintf
        - fmt.Fprintln
        - (*text/tabwriter.Writer).Flush
